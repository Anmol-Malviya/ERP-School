'use client';import {useMemo} from 'react';import {useAuth} from './useAuth';import {getActiveSchool} from '@/lib/api';export function useSchool(){const{session,switchSchool}=useAuth();const activeId=getActiveSchool();const school=useMemo(()=>session?.schools.find(s=>s._id===activeId)||session?.schools[0]||null,[session,activeId]);return{school,schools:session?.schools||[],switchSchool}}
