'use client';import {useEffect,type ReactNode}from'react';import {useRouter}from'next/navigation';import {useAuth}from'@/hooks/useAuth';import type{BackendRole}from'@/lib/auth';export function RoleGuard({allow,children}:{allow:BackendRole[];children:ReactNode}){const{session,loading}=useAuth();const router=useRouter();useEffect(()=>{if(!loading&&(!session||!allow.includes(session.user.role)))router.replace('/login')},[allow,loading,router,session]);if(loading||!session||!allow.includes(session.user.role))return <div className="loading-state">Loading…</div>;return children}
