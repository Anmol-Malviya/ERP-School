'use client';
import {useEffect,useState} from 'react';import {api} from '@/lib/api';import {Icon} from '@/components/ui/Icon';
export function DashboardView(){const[data,setData]=useState<{stats?:{label:string;value:string}[]}|null>(null),[notices,setNotices]=useState<Record<string,unknown>[]>([]),[schedule,setSchedule]=useState<Record<string,unknown>[]>([]),[error,setError]=useState('');useEffect(()=>{Promise.all([api.get<{stats:{label:string;value:string}[]}>('/reports/dashboard'),api.get<Record<string,unknown>[]>('/notices?limit=4'),api.get<Record<string,unknown>[]>('/timetable?limit=6')]).then(([d,n,t])=>{setData(d.data);setNotices(n.data);setSchedule(t.data)}).catch(e=>setError(e.message))},[]);return <><div className="page-heading"><div><span className="eyebrow">Live workspace</span><h1>Dashboard</h1><p>Real-time school data from the ERP backend.</p></div><button className="btn btn-secondary" onClick={()=>location.reload()}><Icon name="clock" size={15}/> Refresh</button></div>{error&&<div className="panel" style={{padding:16,color:'#b91c1c'}}>{error}</div>}<section className="portal-stats">{(data?.stats||[]).map((s,i)=><article className="stat-card" key={s.label}><div className="stat-card-top"><span className="stat-icon"><Icon name={['school','student','teacher','wallet'][i%4] as 'school'} /></span></div><div><p>{s.label}</p><strong>{s.value}</strong></div></article>)}</section><section className="dashboard-layout"><div className="panel"><div className="panel-head"><div><span className="eyebrow">Schedule</span><h2>Timetable</h2></div></div><div className="notice-list">{schedule.length?schedule.map((x,i)=><div className="notice-item" key={i}><span className="notice-icon blue"><Icon name="clock" size={16}/></span><div><strong>{String(x.day||'Schedule')}</strong><p>{Array.isArray(x.periods)?`${x.periods.length} periods`:'Academic timetable'}</p></div></div>):<p className="muted-label">No timetable records yet.</p>}</div></div><div className="panel"><div className="panel-head"><div><span className="eyebrow">Updates</span><h2>Recent notices</h2></div></div><div className="notice-list">{notices.length?notices.map((x,i)=><div className="notice-item" key={i}><span className="notice-icon amber"><Icon name="bell" size={16}/></span><div><strong>{String(x.title||'Notice')}</strong><p>{String(x.body||'')}</p></div></div>):<p className="muted-label">No notices yet.</p>}</div></div></section></>}
