import{api,type ApiEnvelope}from'@/lib/api';export type ListMeta={total:number;page:number;limit:number;pages:number};export const resource=<T,Create=Partial<T>>(path:string)=>({list:(query='')=>api.get<T[]>(`${path}${query?`?${query}`:''}`),get:(id:string)=>api.get<T>(`${path}/${id}`),create:(data:Create)=>api.post<T>(path,data),update:(id:string,data:Partial<T>)=>api.patch<T>(`${path}/${id}`,data),remove:(id:string)=>api.delete<null>(`${path}/${id}`)});export type ResourceResponse<T>=ApiEnvelope<T>;
