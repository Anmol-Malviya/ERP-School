import{api}from'@/lib/api';export type AttendanceRecord={studentId:string;status:'PRESENT'|'ABSENT'|'LEAVE'|'LATE'|'HALF_DAY';remarks?:string};export const attendanceService={list:(query='')=>api.get<Record<string,unknown>[]>(`/attendance${query?`?${query}`:''}`),bulk:(data:{academicSessionId:string;date:string;classId:string;sectionId:string;records:AttendanceRecord[]})=>api.post('/attendance/bulk',data)};
