'use client';
import {useState} from 'react';import {useRouter} from 'next/navigation';import {useAuth} from '@/hooks/useAuth';import {homeFor} from '@/lib/auth';
export default function LoginPage(){const{login}=useAuth(),router=useRouter(),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');try{const s=await login(email,password);router.replace(homeFor(s.user.role))}catch(e){setError(e instanceof Error?e.message:'Login failed')}finally{setBusy(false)}}return <main className="login-page"><section className="login-visual"><div className="login-brand"><div className="login-brand-mark">AV</div><div><strong>School ERP</strong><small>Unified Academic Platform</small></div></div><div className="login-copy"><span>Secure school management</span><h1>One school.<br/>One connected system.</h1><p>Role-based access for administrators, teachers, students and parents.</p></div></section><section className="login-form-side"><form className="login-form" onSubmit={submit}><span className="eyebrow">Secure portal access</span><h2>Welcome back</h2><p>Sign in with the account created by your school.</p>{error&&<p style={{color:'#b91c1c'}}>{error}</p>}<div className="login-field"><label>Email</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></div><div className="login-field"><label>Password</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></div><button className="btn btn-primary login-button" disabled={busy}>{busy?'Signing in...':'Sign in securely →'}</button></form></section></main>}
