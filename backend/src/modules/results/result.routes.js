const express=require('express');const C=require('../../core');const {PERMISSIONS:P}=require('../../constants/permissions');const c=require('./result.controller'),v=require('./result.validator');const router=express.Router(),base=[C.authenticate,C.tenant(true)];router.get('/',...base,C.permission(P.RESULTS_READ),c.list);router.get('/:id',...base,C.permission(P.RESULTS_READ),C.validateId,c.get);router.post('/',...base,C.permission(P.RESULTS_WRITE),...v.create,c.create);router.patch('/:id',...base,C.permission(P.RESULTS_WRITE),C.validateId,c.update);router.post('/:id/publish',...base,C.permission(P.RESULTS_WRITE),C.validateId,c.publish);module.exports=router;
