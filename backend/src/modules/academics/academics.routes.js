const express=require('express');const build=require('../_shared/crud.routes');const {PERMISSIONS:P}=require('../../constants/permissions');const c=require('./academics.controller'),v=require('./academics.validator');const router=express.Router();const mount=(path,key)=>router.use(path,build({controller:c[key],readPermission:P.ACADEMICS_READ,writePermission:P.ACADEMICS_WRITE,validateCreate:v[key].create}));mount('/sessions','sessions');mount('/classes','classes');mount('/sections','sections');mount('/subjects','subjects');module.exports=router;
