const express=require('express');const C=require('../../core');const c=require('./auth.controller'),v=require('./auth.validator');const router=express.Router();router.post('/login',...v.login,c.login);router.post('/refresh',c.refresh);router.get('/me',C.authenticate,c.me);router.post('/logout',C.authenticate,c.logout);router.post('/change-password',C.authenticate,...v.changePassword,c.changePassword);module.exports=router;
