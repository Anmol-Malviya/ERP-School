const express=require('express');const C=require('../../core');const {PERMISSIONS:P}=require('../../constants/permissions');const c=require('./fee.controller'),v=require('./fee.validator'),build=require('../_shared/crud.routes');const router=express.Router();router.use('/fees',build({controller:c.fees,readPermission:P.FEES_READ,writePermission:P.FEES_WRITE,validateCreate:v.fee.create}));const base=[C.authenticate,C.tenant(true)];router.get('/fees-payments',...base,C.permission(P.FEES_READ),c.payments.list);router.get('/fees-payments/:id',...base,C.permission(P.FEES_READ),C.validateId,c.payments.get);router.post('/fees-payments',...base,C.permission(P.FEES_WRITE),...v.payment.create,c.recordPayment);module.exports=router;
