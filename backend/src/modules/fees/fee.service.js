const C=require('../../core');const Fee=require('./fee.model'),Payment=require('./payment.model');const {createCrudService}=require('../_shared/crud.service');const fees=createCrudService({Model:Fee,resource:'Fee',searchFields:['name','category'],filterFields:['academicSessionId','classId','studentId','category','status']});const payments=createCrudService({Model:Payment,resource:'Payment',filterFields:['academicSessionId','feeId','studentId','mode','status'],beforeCreate:async(req,data)=>({...data,receivedBy:req.user._id})});module.exports={fees,payments,async recordPayment(req){const item=await payments.create(req);await C.audit(req,'PAYMENT_RECEIVED','Payment',item._id,{amount:item.amount,receiptNo:item.receiptNo});return item}};
