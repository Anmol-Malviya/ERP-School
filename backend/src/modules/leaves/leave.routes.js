const express=require('express');const C=require('../../core');const {ROLES}=require('../../constants/roles');const {PERMISSIONS:P}=require('../../constants/permissions');const c=require('./leave.controller'),v=require('./leave.validator');const router=express.Router(),base=[C.authenticate,C.tenant(true)];router.get('/',...base,C.permission(P.LEAVES_READ),c.list);router.get('/:id',...base,C.permission(P.LEAVES_READ),C.validateId,c.get);router.post('/',...base,C.permission(P.LEAVES_WRITE),...v.create,c.create);router.post('/:id/review',...base,C.allowRoles(ROLES.SUPER_ADMIN,ROLES.ADMINISTRATOR,ROLES.SCHOOL_ADMIN),C.validateId,...v.review,c.review);module.exports=router;
