const express=require('express');const C=require('../../core');const {ROLES}=require('../../constants/roles');const {PERMISSIONS:P}=require('../../constants/permissions');const c=require('./school.controller'),v=require('./school.validator');const router=express.Router();router.get('/',C.authenticate,C.permission(P.SCHOOLS_READ),c.list);router.get('/:id',C.authenticate,C.permission(P.SCHOOLS_READ),C.validateId,c.get);router.post('/',C.authenticate,C.allowRoles(ROLES.SUPER_ADMIN),...v.create,c.create);router.patch('/:id',C.authenticate,C.permission(P.SCHOOLS_WRITE),C.validateId,c.update);router.delete('/:id',C.authenticate,C.allowRoles(ROLES.SUPER_ADMIN),C.validateId,c.remove);module.exports=router;
