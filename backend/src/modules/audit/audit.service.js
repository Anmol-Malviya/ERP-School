const C=require('../../core');const Audit=require('./auditLog.model');const {ROLES}=require('../../constants/roles');const {tenantBase}=require('../_shared/crud.service');module.exports={async list(req){const {page,limit,skip}=C.page(req.query);const filter=req.user.role===ROLES.SUPER_ADMIN&&!req.tenantId?{}:tenantBase(req);if(req.query.action)filter.action=req.query.action;if(req.query.resource)filter.resource=req.query.resource;const[total,items]=await Promise.all([Audit.countDocuments(filter),Audit.find(filter).sort('-createdAt').skip(skip).limit(limit).lean()]);return{items,meta:{total,page,limit,pages:Math.ceil(total/limit)}}}};
