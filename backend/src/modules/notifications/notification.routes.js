const express=require('express');const C=require('../../core');const c=require('./notification.controller');const router=express.Router();router.use(C.authenticate,C.tenant(false));router.get('/',c.list);router.post('/read-all',c.markAllRead);router.get('/:id',C.validateId,c.get);router.patch('/:id/read',C.validateId,c.markRead);module.exports=router;
