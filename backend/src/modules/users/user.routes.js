const express=require('express');const C=require('../../core');const {PERMISSIONS:P}=require('../../constants/permissions');const c=require('./user.controller'),v=require('./user.validator');const router=express.Router();const base=[C.authenticate,C.tenant(false)];router.get('/',...base,C.permission(P.USERS_READ),c.list);router.get('/:id',...base,C.permission(P.USERS_READ),C.validateId,c.get);router.post('/',...base,C.permission(P.USERS_WRITE),...v.create,c.create);router.patch('/:id',...base,C.permission(P.USERS_WRITE),C.validateId,c.update);router.delete('/:id',...base,C.permission(P.USERS_WRITE),C.validateId,c.remove);module.exports=router;
