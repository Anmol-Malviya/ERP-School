const express=require('express');const C=require('../../core');const {PERMISSIONS:P}=require('../../constants/permissions');const c=require('./attendance.controller'),v=require('./attendance.validator');const router=express.Router(),base=[C.authenticate,C.tenant(true)];router.get('/',...base,C.permission(P.ATTENDANCE_READ),c.list);router.get('/:id',...base,C.permission(P.ATTENDANCE_READ),C.validateId,c.get);router.post('/',...base,C.permission(P.ATTENDANCE_WRITE),...v.create,c.create);router.post('/bulk',...base,C.permission(P.ATTENDANCE_WRITE),...v.bulk,c.bulk);router.patch('/:id',...base,C.permission(P.ATTENDANCE_WRITE),C.validateId,c.update);module.exports=router;
