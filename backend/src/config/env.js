const dotenv=require('dotenv');dotenv.config();
const n=(v,d)=>Number.isFinite(Number(v))?Number(v):d;
const config={nodeEnv:process.env.NODE_ENV||'development',port:n(process.env.PORT,5000),mongoUri:process.env.MONGO_URI||'mongodb://127.0.0.1:27017/erp-school',accessSecret:process.env.JWT_ACCESS_SECRET||'dev-access-secret-change-me',refreshSecret:process.env.JWT_REFRESH_SECRET||'dev-refresh-secret-change-me',accessExpiresIn:process.env.JWT_ACCESS_EXPIRES_IN||'15m',refreshExpiresIn:process.env.JWT_REFRESH_EXPIRES_IN||'7d',corsOrigins:(process.env.CORS_ORIGINS||'http://localhost:3000,http://localhost:3001').split(',').map(x=>x.trim()).filter(Boolean),rateLimitWindowMs:n(process.env.RATE_LIMIT_WINDOW_MS,900000),rateLimitMax:n(process.env.RATE_LIMIT_MAX,300),superAdmin:{name:process.env.SUPER_ADMIN_NAME||'Platform Super Admin',email:process.env.SUPER_ADMIN_EMAIL||'admin@schoolerp.local',password:process.env.SUPER_ADMIN_PASSWORD||'ChangeMe123!'}};
if(config.nodeEnv==='production'&&(config.accessSecret.startsWith('dev-')||config.refreshSecret.startsWith('dev-')))throw new Error('JWT secrets must be configured in production');
module.exports=config;
