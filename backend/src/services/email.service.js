const logger=require('../utils/logger');
async function sendEmail({to,subject,text,html}){if(!to||!subject)throw new Error('Email recipient and subject are required');if(!process.env.EMAIL_WEBHOOK_URL){logger.info('Email provider not configured; message queued/skipped',{to,subject});return{sent:false,reason:'provider_not_configured'}}const response=await fetch(process.env.EMAIL_WEBHOOK_URL,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({to,subject,text,html})});if(!response.ok)throw new Error(`Email provider failed (${response.status})`);return{sent:true}}module.exports={sendEmail};
