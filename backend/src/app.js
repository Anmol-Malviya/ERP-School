const express=require('express');const cors=require('cors');const helmet=require('helmet');const morgan=require('morgan');const {rateLimit}=require('express-rate-limit');const config=require('./config/env');const C=require('./core');
const app=express();app.disable('x-powered-by');app.set('trust proxy',1);app.use(helmet());app.use(cors({origin(origin,cb){if(!origin||config.corsOrigins.includes('*')||config.corsOrigins.includes(origin))return cb(null,true);return cb(new C.ApiError(403,'Origin is not allowed by CORS'))},credentials:true}));app.use(rateLimit({windowMs:config.rateLimitWindowMs,limit:config.rateLimitMax,standardHeaders:'draft-7',legacyHeaders:false}));app.use(express.json({limit:'1mb'}));app.use(express.urlencoded({extended:true,limit:'1mb'}));if(config.nodeEnv!=='test')app.use(morgan(config.nodeEnv==='production'?'combined':'dev'));app.get('/health',(_q,res)=>res.json({success:true,service:'erp-school-api',status:'ok',timestamp:new Date().toISOString()}));app.use('/api/v1',require('./api/router'));app.use((req,_res,next)=>next(new C.ApiError(404,`Route not found: ${req.method} ${req.originalUrl}`)));app.use(C.errorHandler);module.exports=app;
