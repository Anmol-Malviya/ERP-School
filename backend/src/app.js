const express=require('express');const path=require('path');const cors=require('cors');const helmet=require('helmet');const morgan=require('morgan');const config=require('./config/env');const C=require('./core');const notFound=require('./middleware/notFound.middleware');const { publicLimiter, authenticatedLimiter } = require('./middleware/rateLimiters');const app=express();app.disable('x-powered-by');app.set('trust proxy',1);app.use(helmet({crossOriginResourcePolicy:{policy:'cross-origin'}}));app.use(cors({origin(origin,cb){if(!origin||config.corsOrigins.includes('*')||config.corsOrigins.includes(origin))return cb(null,true);return cb(new C.ApiError(403,'Origin is not allowed by CORS'))},credentials:true}));app.post('/api/v1/webhooks/razorpay',express.raw({type:'application/json',limit:'1mb'}),require('./modules/payments/payments.controller').webhookHandler);app.use(express.json({limit:`${Math.ceil(config.uploadMaxBytes/1048576)+2}mb`}));app.use(express.urlencoded({extended:true,limit:'2mb'}));if(config.nodeEnv!=='test')app.use(morgan(config.nodeEnv==='production'?'combined':'dev'));app.use('/uploads',express.static(path.resolve(config.uploadDir),{fallthrough:true,maxAge:config.nodeEnv==='production'?'1d':0}));app.get('/health',publicLimiter,(_q,res)=>res.json({success:true,service:'erp-school-api',status:'ok',timestamp:new Date().toISOString()}));app.use('/api/v1',authenticatedLimiter,require('./api/router'));app.use(notFound);app.use(C.errorHandler);module.exports=app;
