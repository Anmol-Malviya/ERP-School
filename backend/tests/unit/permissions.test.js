const {getRolePermissions,PERMISSIONS}=require('../../src/constants/permissions');const {ROLES}=require('../../src/constants/roles');test('super admin receives platform permission',()=>expect(getRolePermissions(ROLES.SUPER_ADMIN)).toContain(PERMISSIONS.PLATFORM_MANAGE));test('student cannot write fees',()=>expect(getRolePermissions(ROLES.STUDENT)).not.toContain(PERMISSIONS.FEES_WRITE));
