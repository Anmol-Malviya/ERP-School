import{api}from'@/lib/api';import type{School}from'@/types/school';export const schoolService={list:(query='')=>api.get<School[]>(`/schools${query?`?${query}`:''}`),get:(id:string)=>api.get<School>(`/schools/${id}`),onboard:(data:Record<string,unknown>)=>api.post<School>('/onboarding/school',data),update:(id:string,data:Partial<School>)=>api.patch<School>(`/schools/${id}`,data),remove:(id:string)=>api.delete<null>(`/schools/${id}`)};
