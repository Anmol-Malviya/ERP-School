import{api}from'@/lib/api';import type{Administrator}from'@/types/administrator';export const administratorService={list:()=>api.get<Administrator[]>('/users?role=ADMINISTRATOR'),onboard:(data:Record<string,unknown>)=>api.post<Administrator>('/onboarding/administrator',data),update:(id:string,data:Partial<Administrator>)=>api.patch<Administrator>(`/users/${id}`,data),remove:(id:string)=>api.delete(`/users/${id}`)};
