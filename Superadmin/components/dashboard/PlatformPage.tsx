'use client';import {usePathname} from 'next/navigation';import {platformConfigs} from '@/lib/platform-config';import {PlatformCrud} from './PlatformCrud';import {PlatformDashboard,PlatformAnalytics,PlatformAudit,PlatformSettings} from './PlatformDashboard';export default function PlatformPage(){const name=usePathname().split('/').filter(Boolean).pop()||'dashboard';if(name==='dashboard')return <PlatformDashboard/>;if(name==='analytics')return <PlatformAnalytics/>;if(name==='audit-logs')return <PlatformAudit/>;if(name==='settings')return <PlatformSettings/>;if(platformConfigs[name])return <PlatformCrud name={name}/>;return <PlatformDashboard/>}
