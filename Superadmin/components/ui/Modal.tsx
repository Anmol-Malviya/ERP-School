'use client';import type{ReactNode}from'react';export function Modal({open,title,onClose,children}:{open:boolean;title:string;onClose:()=>void;children:ReactNode}){if(!open)return null;return <div className="modal-backdrop" onMouseDown={onClose}><section className="surface-card modal-card" role="dialog" aria-modal="true" aria-label={title} onMouseDown={e=>e.stopPropagation()}><div className="card-heading-row"><h3>{title}</h3><button type="button" className="more-button" onClick={onClose}>×</button></div>{children}</section></div>}
