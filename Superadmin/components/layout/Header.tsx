'use client';import {Icon} from '../ui/Icon';import {useAuth} from '@/hooks/useAuth';import {initials} from '@/lib/auth';export function Header({onMenu}:{onMenu:()=>void}){const{session}=useAuth();return <header className="topbar"><button className="icon-button mobile-menu" onClick={onMenu}><Icon name="menu"/></button><div className="topbar-search"><Icon name="search" size={18}/><input placeholder="Search platform records..."/><kbd>⌘ K</kbd></div><div className="topbar-actions"><button className="icon-button notification-button"><Icon name="bell" size={19}/><span/></button><div className="divider"/><button className="profile-button"><span className="avatar">{initials(session?.user.name)}</span><span className="profile-copy"><strong>{session?.user.name}</strong><small>Platform Owner</small></span></button></div></header>}
